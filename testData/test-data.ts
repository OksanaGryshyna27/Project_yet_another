import { USER_EMAIL, USER_PASSWORD, API_BASE_URL, BASE_URL } from "../config/baseConfig";

export const URLS = {
    API_BASE_URL,
    BASE_URL
}
export const UserData = {
    USER_EMAIL,
    USER_PASSWORD
}
